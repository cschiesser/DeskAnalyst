# LLM / embedding provider keys (fill in what you use)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# SEC EDGAR requires a descriptive User-Agent with contact info,
# or requests get throttled / blocked. Format: "app-name your-email"
SEC_USER_AGENT=DeskAnalyst Colin-Schiesser colin.sch@bluewin.ch
